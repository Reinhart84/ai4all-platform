name: Deploy to Firebase Hosting and Functions

on:
  push:
    branches:
      - main
      - staging

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          # Node 20 is vereist voor firebase-tools v14+
          node-version: '20'

      # Installeer de dependencies voor de Cloud Functions
      - name: Install functions dependencies
        working-directory: functions
        run: npm install

      # Bouw de React‑frontend
      - name: Build React app
        working-directory: frontend
        run: |
          npm install
          npm run build

      # Deploy Firebase Functions alleen vanuit de main‑branch
      - name: Deploy Cloud Functions
        if: github.ref == 'refs/heads/main'
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/service-account.json
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > ${{ github.workspace }}/service-account.json
          npm install -g firebase-tools
          firebase deploy --only functions --project ai4all-platform-fe2e4

      # Deploy Hosting naar de live omgeving bij main
      - name: Deploy Hosting (prod)
        if: github.ref == 'refs/heads/main'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT }}"
          channelId: live
          projectId: ai4all-platform-fe2e4

      # Deploy Hosting naar staging-kanaal bij staging
      - name: Deploy Hosting (staging)
        if: github.ref == 'refs/heads/staging'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT }}"
          # Kies hier een naam voor je staging‑kanaal
          channelId: staging
          projectId: ai4all-platform-fe2e4
